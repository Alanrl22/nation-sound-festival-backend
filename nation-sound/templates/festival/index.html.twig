{% extends 'base.html.twig' %}

{% block title %}Hello FestivalController!{% endblock %}

{% block body %}
<div class="container artist">
    <h1>Créer un festival</h1>

    {{ form(festivalForm) }}
</div>

<div>
   
 {% for festival in festivals %}
        <p><a href="{{ path('festival_edit', {'id':festival.id}) }}">{{ festival.name }}</a></p>
    {% endfor %}
</div>

{% endblock %}