{% extends 'base.html.twig' %}

{% block title %}Hello ContactController!{% endblock %}

{% block body %}
<div class="container contact">
    <h1>Ajouter un contact</h1>

    {{ form(contactForm) }}
</div>

<div>
   
 {% for contact in contacts %}
        <p><a href="{{ path('contact_edit', {'id':contact.id}) }}">{{ contact.name }}</a></p>
    {% endfor %}
</div>

{% endblock %}
